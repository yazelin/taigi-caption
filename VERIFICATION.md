# 驗證紀錄

這份檔案記錄本專案「實際量過」的數字與觀察,不是引用別人的規格表。
每一條都附當時的量法,任何人可以自己重跑。跑法見 `bench.py` 與下面各段。

量測環境:Ubuntu 24.04、NVIDIA GeForce RTX 4060 Laptop(8GB)、driver 580.173.02、CUDA 12.6、
模型 `WizardForest/faster-whisper-Breeze-ASR-26-int8`(CTranslate2 int8 量化,1.56GB)。
量測日期 2026 年 7 月 30 日。

## 一、模型真的能把台語講成繁體中文

測試素材是自己合成的三句台語(意傳媠聲 demo 端點,內容由我們指定,所以對照答案已知)。
這是寬鬆條件,原因見第六節;真人音檔的結果在第三之二節。

| 台語原句 | 華語對照(我們給的) | 辨識輸出(language=zh) |
| --- | --- | --- |
| 今仔日天氣真好 | 今天天氣真好 | 今天天氣很好 |
| 你食飽未 | 你吃飽了嗎 | 你吃飽了嗎 |
| 我明仔載欲去台北出差 | 我明天要去台北出差 | 我明天要去台北出差 |

方向確認:輸入台語語音,輸出的是華語用字的繁體中文,不是台語正字。
這正是本願望要的(給不懂台語的人讀),也符合模型自己的說明。

## 二、語言 token 到底要給什麼

背景:這顆模型的 `config.json` 把 `forced_decoder_ids` 寫成 `<|en|>`(50259),
`generation_config.json` 的語言槽卻是 `null`,官方 model card 沒有 usage 範例。
所以我們沒有猜,直接三種都跑。

第一輪只用 3 段合成語音,量到 `zh` 5.6%、`auto` 5.6%、`en` 13.3%,當時的結論是「預設 zh」。
後來拿到 14 段行政院真人台語廣播與官方華語腳本,樣本變成 17 段,**結論就反過來了**。
這裡把兩輪都留著,因為它本身就是「三個樣本不能下結論」的例子。

17 段(14 真人 + 3 合成)、套用與 production 相同的防幻聽過濾器:

| 設定 | 平均 CER | 中位數 | 平均耗時 |
| --- | --- | --- | --- |
| `en` | 29.7% | 31.4% | 2495ms |
| `zh` | 30.4% | 30.1% | 2638ms |
| `auto`(不指定) | 34.9%(未套過濾器時的相對值) | 不適用 | 3838ms |

成對比較(逐段比誰的 CER 低):**`en` 勝 10 段、`zh` 勝 4 段、平手 3 段**。
差距最大的兩段都是 `en` 贏:1957 社福專線篇 25.8% 對 41.4%、主計總處人力調查篇 14.1% 對 22.2%。

結論:

- 預設改用 `en`。它不是筆誤,語言 token 只是解碼時的條件,輸出仍然是繁體中文。
  兩個獨立線索都指向它:模型 `config.json` 的 `forced_decoder_ids` 寫的就是 `<|en|>`(50259),
  推測是微調當時的設定;實測成對比較也是 en 較好。
- 但誠實說:平均只差 0.7 個百分點,中位數反而略偏 `zh`,17 段的樣本不足以稱定論。
  所以 `TAIGI_LANG` 是環境變數,換 `zh` 完全合理,請用自己的音檔跑 `bench.py` 決定。
- `auto` 不建議:多花約 45% 時間做語言偵測,而且偵測結果是錯的,
  回報 `my`(緬甸語)、信心值只有 0.13 到 0.17,輸出卻仍是正常中文。

## 三、資源與延遲

| 項目 | 實測 |
| --- | --- |
| 模型載入 | 1.5 秒 |
| 佔用 VRAM | 約 2.0GB(基線 1184MiB → 載入後 3203MiB) |
| 第一次推論 | 728ms(冷) |
| 之後推論 | 374 到 381ms(2 秒音檔) |
| 22 秒音檔 | 1039ms,單一 segment,九句全對,沒有退化重複 |

兩個推論:伺服器啟動後要先餵一段空音檔暖機,否則第一位使用者會等到冷啟動;
另外段落越長單位效率越好,不需要把音檔切得太碎。

## 三之二、真人台語音檔實測

素材是行政院公共服務廣播音檔的台語版共 14 段,每段約 30 秒,真人錄音
(testdata 裡另有 3 段自己合成的短句,合計 17 段)。
這批音檔正是 Breeze-ASR-26 官方 benchmark 的素材來源,所以是很接近官方條件的真實測試。

速度:正常情況每 30 秒音檔耗時 1.5 到 3.3 秒(即時倍率 0.05 到 0.11),遠快於即時。

品質是「意思抓得到,但錯字明顯」,舉三個實例(輸出照抄,錯字沒有修):

- 教育部台語輸入法篇,幾乎全對:「我想教孩子台灣台語 但是要怎麼打字 不用煩惱 用說的就好了
  教育部推出 台灣台語輸入法App 透過語音辨識 用手機說台灣台語 就可以轉成文字」
- 潛伏結核篇,關鍵名詞持續錯成同一個音近詞:「陳太太 妳被檢查出來 是前腹結核的感染者」
  (應為潛伏結核),但整段語意仍然清楚。
- 國慶大會篇,出現會誤導的錯字:「請教師朋友配合改走別條路」,原意應是「駕駛朋友」;
  「國慶歌夢」應為國慶官網。

結論寫進 README 的期待管理:這個工具足以讓不懂台語的人當場抓到意思,
但不能當正式紀錄,遇到人名、機關名、專有名詞要特別留意。

## 四、雜訊與句尾會產生幻聽,而 `no_speech_prob` 抓不到

這是本專案最重要的防線。餵入沒有人聲的音訊時,模型會編出句子:

| 輸入 | avg_logprob | no_speech_prob | 輸出 |
| --- | --- | --- | --- |
| 真語音三句 | -0.13 / -0.06 / -0.03 | 0.00 | 正確 |
| 22 秒連續語音 | -0.02 | 0.00 | 正確 |
| 數位靜音 3 秒 | 無 segment | 無 | 空字串(正常) |
| 低幅雜訊 | -1.27 | 0.00 | 「二十四小時後...」 |
| 中幅雜訊 | -1.75 | 0.00 | 「這...」 |
| 高幅雜訊 | -1.51 | 0.00 | 「快點...」 |
| 隨機雜訊 | -1.42 | 0.00 | 「爸,辛苦你了」 |

結論:

- `no_speech_prob` 在這顆模型上恆為 0.00,完全不能當判斷依據。
- `avg_logprob` 可以:真語音落在 -0.13 以上,雜訊落在 -1.27 以下,中間空隙很大。
- 因此後端用 `avg_logprob` 門檻擋掉幻聽,預設 -1.0(留在空隙中間偏保守側),
  可用 `TAIGI_MIN_LOGPROB` 調整。
- 誠實補充:上表的真語音是乾淨合成音。真人在有背景聲的房間講話,logprob 會比 -0.13 低,
  所以門檻設太靠近 -0.13 會誤殺真句子。若發現真的講話卻沒出字,把門檻調更負(例如 -1.5)。

### 幻聽長在哪裡:逐段過濾比整段丟棄精準得多

真人音檔上發現一個穩定的規律:幻聽幾乎都獨立長成「最後一個很短的 segment」,
而且它的 logprob 與真內容差距很大。同一個檔案裡:

| 檔案 | 真內容段 | 幻聽尾段 |
| --- | --- | --- |
| 國慶大會篇 | 0.0 到 30.0 秒,lp -0.17 | 30.0 到 30.8 秒,lp -1.29,內容「我...」 |
| 省電篇 | 0.0 到 30.0 秒,lp -0.14 | 30.0 到 30.1 秒,lp -1.52,內容「我會好好地珍惜 將來的日子」 |

成因是 Whisper 以 30 秒為一個窗,音檔尾巴剩下的零點幾秒被填充成一個新窗,模型就對著填充值編故事。
重複迴圈型的幻聽(例如「吃飯了... 吃飯了... 吃飯了...」)另外會讓 `compression_ratio` 衝到 2.24。

所以設計改成逐段過濾,而不是判斷整次回應:丟掉 `avg_logprob` 低於門檻、或 `compression_ratio` 高於 2.4 的 segment,
留下的接起來就是字幕。

驗證方式:對 14 段行政院真人廣播全跑一次,統計留下與丟掉的段落,並人工看過每一段被丟掉的內容。
兩種語言 token 都跑過,兩次的結論一致:**被丟掉的每一段都是肉眼可見的幻聽,沒有一段真內容被誤殺。**

用 production 預設的 `en`:總共 25 段輸出,留 18 段、丟 7 段。丟掉的七段是

「you」(-1.07)、「you」(-1.12)、「I don't kn...」(-1.55)、「you」(-1.32)、
「N-N-N-N-N-N-N-N-N」(-1.16)、「? ?」(-1.5)、「you」(-1.21)。

用 `zh` 跑同一批(當時還多了一段意傳示範音,共 15 檔、27 段輸出),丟 9 段:

「我...」(-1.29)、「我...」(-1.35)、「好,媽,妳先去休息...」(-1.47)、「老闆」(-1.58)、
「我會好好地珍惜這段時間」(-1.64)、「夾娃娃送天鳴」(-1.97)、「來」(-1.02)、
「我會好好珍惜這個機會」(-1.71)、「謝謝」(-1.66)。

順帶一個觀察:`en` 的尾端幻聽是英文與亂碼,`zh` 的尾端幻聽是通順的中文句子。
後者更危險,因為「我會好好珍惜這個機會」看起來就像使用者真的講了那句話,
萬一沒被擋掉,讀字幕的人不會知道那是模型編的。這是選 `en` 的一個額外理由,但不是主要理由。

邊界誠實說明:最接近門檻的是 `zh` 那次的「來」,-1.02,離預設門檻 -1.0 只差 0.02
(`en` 那次最接近的是 -1.07)。真人講話若剛好落在 -1.0 附近就會被誤殺,
這也是為什麼門檻要留成環境變數。

這道過濾器對數字的貢獻也量得出來(同一批 17 段音檔,只差有沒有套過濾器):

| 語言 token | 不套過濾器 | 套過濾器 |
| --- | --- | --- |
| `en` | 31.9% | 29.7% |
| `zh` | 32.2% | 30.4% |

也就是大約兩個百分點。`bench.py --no-filter` 可以自己重跑這個對照。
更重要的是使用者感受:被丟掉的那些是「以上廣告由財政部提供**元勇**」、
「行政院主計總署公告**請不吝點贊 訂閱 轉發 打開小鈴鐺 以便接收更多遊戲資訊**」這種句子
(後者顯然是訓練語料裡的影片結尾語殘留),看到它們出現在字幕上比 CER 高兩個百分點難接受得多。

## 四之二、切在句中會產生「過濾器抓不到」的亂句

這是決定分段策略的實驗。拿同一段真人音檔(教育部台語輸入法篇,30 秒),
分別用固定 3 秒、固定 6 秒切段後逐段辨識,再與整段一起辨識的結果比較。

整段一起跑(接近正解):
「我想教孩子台灣台語 但是要怎麼打字 不用煩惱 用說的就好了 教育部推出 台灣台語輸入法App
透過語音辨識 用手機說台灣台語 就可以轉成文字」

固定 6 秒切段的結果就壞了,出現整句捏造:
「唉 妳在煩惱什麼 我想教孩子臺語 但是... 怎麼打字啊 別擔心 用說的就好了 到部隊
**採訪撰稿人 金汝外交官** 不如轉成日文 也可以到App Store 或是谷歌Play平台...」

固定 3 秒切段沒那麼糟但邊界處持續掉字:
「台灣台語書字法App」(輸入法)、「櫃位下載」(谷歌Play)、「以上廣告有夠久」(由教育部)。

關鍵觀察:那句完全捏造的「採訪撰稿人 金汝外交官」avg_logprob 是 **-0.93**,
高於我們的 -1.0 門檻,所以逐段過濾**擋不住它**。
也就是說 logprob 門檻能擋掉「對著靜音或雜訊編故事」,但擋不掉「把切斷的半句話硬讀成別的詞」。

所以分段策略的結論是:

- 一定要切在靜音處(前端 VAD 判斷句子講完才送),不能按固定秒數切。
- 硬上限(避免一直講不切)不可避免,但觸發時要挑最近一段時間內音量最低的位置下刀,不要正好切在時間到的那一刻。
- README 必須誠實寫出這個限制:講很長又完全不停頓時,字幕可能出現讀錯的句子。
- 附帶資料:真人廣播 30 秒內只有兩處超過 0.4 秒的停頓(專業配音又壓了配樂),
  所以硬上限在這種音源上會常常觸發;一般對話的停頓比這密得多,情況會好很多。

## 四之三、把上一句字幕當上下文餵回去,能救回被切壞的句子

接著上一個實驗:同樣的固定 6 秒切段,只多做一件事,把「前一段辨識出來的文字」
當成 `initial_prompt` 傳給下一段。結果差異很大。

| 音檔片段 | 不給上下文 | 給上一句當上下文 |
| --- | --- | --- |
| c6_01 | 「怎麼打字啊 別擔心 用說的就好了 到部隊」lp -0.43 | 「要怎麼打字啊 別擔心 用說的就好了 到了部隊...」lp -0.41 |
| c6_02 | 「採訪撰稿人 金汝外交官」lp -0.93(整句捏造) | 「台灣台語輸入法App 透過語音辨識 用手機說台灣台語就能辦到」lp -0.24 |
| c6_03 | 「不如轉成日文 也可以到App Store...」lp -0.44 | 「不只可以轉成日文 也可以到App Store...」lp -0.37 |
| c6_04 | 「就可以下財了 以上廣告由教育部提供」lp -0.19 | 「就可以下載了 以上廣告由教育部提供」lp -0.08 |

那句最嚴重的捏造被完全修正,而且每一段的 logprob 都變好。

所以實作上:後端接一個選填的上下文文字,前端把「上一句已確定的字幕」傳回來。
兩個保護措施(避免上下文反過來害事):

- 只傳最後約 200 字,而且只傳「通過過濾器的字幕」。若上一句被判定為幻聽而丟掉,就不要拿它當上下文,否則會把錯誤傳染下去。
- 重複迴圈仍由 `compression_ratio` 門檻擋。

仍然存在的錯誤(誠實紀錄):「到了部隊」其實是「教育部推出」,「不只可以轉成日文」其實是「就可以轉成文字」。
上下文能救回整句捏造,但救不回所有詞。

## 五、純 CPU:不適合即時字幕,但很適合整段轉寫

第一輪只量了短句就下結論「CPU 慢於即時」,那個講法太粗糙,後來補量才看清楚真正的形狀。

關鍵是 Whisper 不論你餵多短的音檔,都會補滿成一個 30 秒的窗去跑編碼器。
所以**每次請求有一個固定成本,與音檔長度幾乎無關**,只有解碼那部分會隨字數增加:

| 音檔長度 | CPU 耗時(int8,4 執行緒) |
| --- | --- |
| 1.9 秒 | 4.8 秒 |
| 5.0 秒 | 4.6 秒 |
| 10.0 秒 | 6.1 秒 |
| 29.9 秒 | 9.4 秒 |

換算成即時倍率,同一台機器會得到兩個完全相反的結論:

| 用法 | 即時倍率(2 執行緒) | (4 執行緒) | (8 執行緒) |
| --- | --- | --- | --- |
| 短句連續辨識(即時字幕) | 4.35 倍慢 | 2.57 倍慢 | 3.64 倍慢 |
| 整段 30 秒一次跑(離線轉寫) | 0.44 倍(快 2.3 倍) | 0.28 倍(快 3.6 倍) | 0.36 倍(快 2.8 倍) |

所以:

- **即時字幕不能用 CPU。** 一句話講完要等四到八秒才出字,而下一句已經在講了,只會越積越多。
  這個用途實質上需要一張 NVIDIA GPU。
- **整段轉寫用 CPU 很划算。** 30 秒音檔在四執行緒上 9.4 秒就跑完,比即時快三倍多。
  「錄一段丟進去、等十幾秒拿逐字稿」這種 UX,純 CPU 完全撐得住,連兩核心都可以(30 秒音檔 13 秒)。
- 執行緒開越多不一定越快:8 執行緒反而比 4 執行緒慢一點,量測環境上兩者差距不大,
  但可以確定不必為了這個去堆核心數。

## 五之一之二、走空氣的真實聲學路徑

前面所有測試都是把音檔直接送進程式,等於跳過了整條實體聲學路徑。
這一段補上真實情況:**音檔從喇叭放出來,經過房間,再由 USB 麥克風收進來**。
量測用的是同一段教育部台語輸入法篇廣播。

先確認迴路本身:麥克風錄到的訊號 RMS 0.0114、峰值 0.133,確實收得到。

**單次整段辨識(把麥克風錄到的 30 秒一次丟給模型)**,與直接餵原始檔比較:

| | 內容 |
| --- | --- |
| 直接餵檔案 | 「...用說的就好了 **教育部推出 台灣台語輸入法App** 透過語音辨識...」 |
| 走空氣再辨識 | 「...用講的就好了 **到下午 催促臺語師日發App** 透過語音辨識...」 |

意思整段仍在,但**專有名詞被打壞**:「教育部推出 台灣台語輸入法App」變成「到下午 催促臺語師日發App」。
這是直接餵檔案完全看不到的劣化,也是真實使用會遇到的。

**但走完整 app(VAD 切句 + 上下文串接)之後反而修回來了。** 同一個聲學條件下,畫面上出現的三行字幕是:

1. 「唉 妳是在煩惱什麼 我想要教孩子臺語 但是要怎麼打字 唉唷 不用煩惱 用講的就好了 下午催回來」
2. 「**臺語輸入法App** 透過語音辨識 用手機講臺語 就可以轉成文字 也可以去App Store 或是谷歌會的賣場 找**臺語輸入法App**」
3. 「就可以下載了 以上廣告由教育部提供」

「臺語輸入法App」兩次都對。推測原因是切在靜音處讓每一段都是完整語意單位,
加上前一句被當成上下文餵回去,兩者合起來把單次整段辨識救不回來的專有名詞救了回來。
這是第四之二、四之三兩節設計決策在真實聲學條件下的驗證,不只是切檔案的實驗結論。

錄影腳本會同時螢幕錄影,所以這一段測試的產物就是 demo 影片本身。
誠實邊界:仍然是預錄的專業配音經過喇叭,不是真人當場對著手機講話;真人口語沒有測過。

## 五之二、端到端驗證:真瀏覽器加假麥克風,整條路都通

`scripts/e2e-check.mjs` 會開一個真的瀏覽器,用假麥克風播放一段真人台語音檔,
走完整條路(Web Audio 收音 → 重取樣 → VAD 切段 → 前端包 WAV → POST /transcribe →
模型辨識 → 過濾幻聽 → 畫面出字幕),然後斷言畫面上真的出現中文字幕。

結果:連續兩回都在第一次嘗試就通過,畫面顯示
「唉 妳是在煩惱什麼 我想要教孩子臺語 但是要怎麼打字」(音源是教育部台語輸入法篇)。

過程中踩到四個坑,都寫進腳本註解免得下一個人重踩:

1. **headless chromium 不支援音訊擷取。** 安全環境、`navigator.mediaDevices`、裝置列舉全部正常,
   但 `getUserMedia({audio:true})` 直接回 `NotSupportedError: Not supported`。
   換成真 Chrome(channel chrome)加有頭模式就正常,所以這支測試需要有 X display。
2. **測試不能先「預熱」麥克風。** 兩條擷取串流同時存在時,Chrome 的假檔案音源只餵其中一條,
   預熱反而讓 app 自己那條收不到任何 frame(畫面停在「聆聽中」但 frame 數是 0)。
   而且預熱後把 track stop 掉,假麥克風就不再餵音訊給下一條串流。
3. **不能用 Playwright 的 `waitForFunction`。** 它會在頁面裡以字串求值,
   而 `index.html` 的 CSP 是 `script-src 'self' blob:`(不允許 unsafe-eval),
   於是等待條件永遠不會成立,還會丟 `EvalError`。CSP 是刻意的,所以改成自己輪詢 `page.evaluate`。
   這也是個教訓:斷言寫錯的話會假通過。第一版斷言讀整個字幕區的 `innerText`,
   結果抓到開場提示文字就宣告通過,實際上一句字幕都沒出現。
4. **自動化環境下第一次點擊有機率沒生效**(狀態一直停在「待機」,像是點擊被吞掉)。
   原因沒查清楚,service worker 並沒有強制重載頁面,所以還不能歸咎於它。
   腳本改成按下後確認 `data-on` 真的變了,沒變就再按,最多三次。
   附帶的產物:app 端替 `getUserMedia` 加了 15 秒逾時,
   因為實測 Chrome 偶爾會在取得音訊裝置時整個卡住,promise 既不 resolve 也不 reject,
   沒有逾時的話使用者只會看到畫面永遠停在「正在要麥克風權限」。

## 五之二之二、手機視埠實測

願望要的是手機工具,所以用 iPhone 13 的視埠(390x664)重跑一次同一條端到端流程,
走 https 自簽憑證位址、用 tap 而不是 click:

| 項目 | 結果 |
| --- | --- |
| 字幕是否出現 | 有,「唉 妳是在煩惱什麼 我想要教孩子臺語 但是要怎麼打字」 |
| 橫向溢出 | 沒有 |
| 字幕字級 | 28.08px |
| 開始鈕高度 | 64px(手指好按) |

截圖是 `e2e-shot-mobile.png`(不進版本控制)。
誠實補充:這是 Chrome 的手機視埠模擬,不是真的 iOS Safari。
iOS 上的 AudioWorklet 與麥克風權限行為要在真機才算驗過,目前沒有真機測過。

## 五之三、手機真正要走的那條路:HTTPS 與自簽憑證

手機瀏覽器只在 HTTPS 或 localhost 給麥克風權限,所以區網用 http 位址是行不通的。
這一段把兩種情況都驗過:

- **http 走區網 IP**:`window.isSecureContext` 是 false,app 會顯示
  「瀏覽器只在 HTTPS 或 localhost 允許使用麥克風」、把開始鈕停用、狀態改成「這個網址不能用麥克風」。
  這是刻意的:與其讓使用者按下去才失敗,不如一開始就說清楚。
- **https 走 `scripts/make-cert.sh` 產的自簽憑證**:端到端驗證通過,字幕正常出現。

但自簽憑證有一個真實的限制,實測抓到的:**Chrome 不讓有憑證錯誤的來源註冊 service worker**
(主控台會出現 An SSL certificate error occurred when fetching the script)。
也就是說用自簽憑證時,PWA 的離線外殼與「加到主畫面」不會生效,只有字幕功能本身能用。
要完整的 PWA 行為需要一張被信任的憑證(自己的網域,或 Tailscale 之類會發真憑證的通道)。

## 五之四、前端放 GitHub Pages、後端留在使用者電腦,這條路實測可行

這是為了「給人試玩」找出來的組合:前端是公開的 HTTPS 靜態頁,後端跑在使用者自己的電腦上。
好處是使用者不必安裝前端、也不必處理自簽憑證。

實測結果:**整條路通**。從 `https://yazelin.github.io/taigi-caption/app/?api=http://127.0.0.1:8002`
開頁,用假麥克風播真人台語音檔,畫面確實出現字幕,後端 `/health` 回報 `device: cuda`。

但有一個一定會遇到、而且很容易被誤判成故障的東西:

**Chrome 會要求「存取本機網路裝置」的權限。** 從公開網站去打 `127.0.0.1` 屬於跨位址空間的請求,
使用者按拒絕的話,請求會被擋掉,主控台訊息是
`Access to fetch ... has been blocked by CORS policy: Permission was denied for this request to access the loopback address space.`

這件事是在自動化測試裡踩出來的,而且踩法本身值得記下來:
**Playwright 的 `permissions` 清單是整組取代的**,傳 `permissions: ['microphone']` 會把清單外的權限
一律設成拒絕,連帶拒掉本機網路存取。所以 `scripts/e2e-check.mjs` 刻意不傳 `permissions`,
麥克風改由 `--use-fake-ui-for-media-stream` 自動同意。
一開始沒想通這點,誤以為是 service worker 攔截造成的,還特地驗過一輪
(結論:service worker 接管與否都不影響,四種情況都成功),白繞了一圈。

對使用者的意義:會跳兩個權限視窗(麥克風、本機網路裝置),兩個都要允許。這已經寫進說明頁。

誠實邊界:只在 Chrome 測過。Safari 與 Firefox 對本機網路存取的處理不同,沒測過。
另外這條路只適用電腦:手機開這個網址時 `127.0.0.1` 指的是手機自己。

## 六、這些數字的邊界(不要過度解讀)

- 第一、二節的素材是合成語音,而 Breeze-ASR-26 的訓練語料本身就是合成語音,
  所以 5.6% CER 是最寬鬆的條件,不能拿來宣稱真人準確度。
- 官方在 Taigi ASR Benchmark(行政院公共服務音檔,30 組台華平行語料)量到的平均 CER 是 30.13%,
  單筆最好 14.49%、最差 52.78%。真人自然口語請以這個量級為預期。
- 語言 token 的成對比較只有 17 段,`en` 勝 10 場是傾向而不是定論,別當成選型結論。
- CT2 int8 是社群轉檔版本,與原始 F32 權重的差異我們沒有逐段比對過(本機磁碟不足以同時放兩份)。
  間接證據是量到的 CER 與官方公布值同一量級,所以量化沒有明顯損失,但這不等於逐段等價。
- 14 段真人素材是官方 benchmark 30 組裡的一部分,而且對照文稿經過正規化
  (拿掉說話者標記與音效指示,否則沒唸出來的字會被算成錯誤),逐字原文保留在
  `testdata/manifest-ey.json` 的 `ref_text_verbatim` 欄位。數字上還有一兩個百分點的噪音來自
  數字與英文的唸法差異(例如「全民+1」唸成「加一」、`10000.gov.tw`、`1957`、`LED`、`APP`)。
- 所有測試都是單一說話者、單支手機的情境。多人對話、遠場收音、吵雜環境都沒有測。
